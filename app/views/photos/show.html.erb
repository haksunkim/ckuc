<%= image_tag @photo.photo_url, alt: @photo.tag, style: "padding:10px;width:100%;height:auto;" %>
<%= raw("<br/>#{@photo.tag}") %>
<br/>
<div align="center">
  <%= link_to 'Edit', edit_photo_path(@photo), class: "btn btn-primary" %>
  <%= link_to 'Back', photos_path, class: "btn btn-default" %>
</div>