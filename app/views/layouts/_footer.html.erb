<footer class="footer">
	<div class="container">
		<div class="col-md-4">
			<h3><b><%= t('footer.focus.title') %></b></h3>
			<p><%= raw(t('footer.focus.content')) %></p>
		</div>
		<div class="col-md-4">
			<h3><b><%= t('footer.schedule.title') %></b></h3>
			<p><%= raw(t('footer.schedule.content')) %></p>
		</div>
		<div class="col-md-4">
			<h3><b><%= t('footer.contact.title') %></b></h3>
			<p><%= raw(t('footer.contact.content')) %></p>
		</div>
	</div>
	<!--small> <%= t('home_title')%> </small>
	<nav class="container">
		<ul>
			<% if user_signed_in? %>
			<li>
				<%= link_to "Users", "#" %>
			</li>
			<li class="dropdown">
				<a href="#" class="dropdown-toggle" data-toggle="dropdown">Account <b class="caret"></b></a>
				<ul class="dropdown-menu">
					<li>
						<%= link_to "Profile", users_show_path %>
					</li>
					<li>
						<%= link_to "Settings", edit_registration_path(current_user) %>
					</li>
					<li class="divider"></li>
					<li>
						<%= link_to "Log out",  destroy_user_session_path , method: "delete" %>
					</li>
				</ul>
			</li>
			<% else %>
			<li>
				<%= link_to "Log in",  new_user_session_path %>
			</li>
			<% end %>
			<li>
				<%= link_to "About", about_path %>
			</li>
		</ul>
	</nav-->
</footer>